import { FC } from 'react';

const Navbar: FC = () => {
    return (
        <div className={'w-screen bg-slate-700 text-gray-50 p-5'}>
            <div className={'text-2xl font-bold'}>Employee Management System</div>
        </div>
    );
};

export default Navbar;
